import { StickyHeader } from "@/components/common/StickyHeader";
import Button from "@/components/mt/Button";

export default function Homepage() {
  return (
    <div>
      <StickyHeader />
      <Button> Normal </Button>
    </div>
  );
}
